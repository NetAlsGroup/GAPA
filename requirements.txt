# Beginner/core dependency set.
# For all runtime features use:
#   pip install -r requirements/full.txt
# For development tools as well use:
#   pip install -r requirements/dev.txt
-r requirements/core.txt
